const uploadTagImageMethod = 'biketag.content.uploadTagImage'
describe(uploadTagImageMethod, () => {
  test.todo(`${uploadTagImageMethod} method requires ImgurHash from payload`)

  test.todo(
    `${uploadTagImageMethod} method requires a valid uploadTagImagePayload`
  )

  test.todo(`${uploadTagImageMethod} method resolves data of type Tag[]`)

  test.todo(
    `${uploadTagImageMethod} method resolves status of HttpStatusCode.Ok`
  )
})
