const imgurUploadTagImageMethod = 'biketag.images.uploadTagImage'
describe(imgurUploadTagImageMethod, () => {
  test.todo(
    `${imgurUploadTagImageMethod} method requires ImgurHash from payload`
  )

  test.todo(
    `${imgurUploadTagImageMethod} method requires a valid uploadTagImagePayload`
  )

  test.todo(`${imgurUploadTagImageMethod} method resolves data of type Tag[]`)

  test.todo(
    `${imgurUploadTagImageMethod} method resolves status of HttpStatusCode.Ok`
  )
})
