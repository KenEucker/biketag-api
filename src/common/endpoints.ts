export const BIKETAG_API_PREFIX = 'https://api.biketag.org'

export const API_VERSION = '2'

export const AUTHORIZE_ENDPOINT = 'oauth2/authorize'

export const GAMES_ENDPOINT = `${API_VERSION}/games`

export const AMBASSADORS_ENDPOINT = `${API_VERSION}/ambassadors`

export const PLAYERS_ENDPOINT = `${API_VERSION}/players`

export const TAGS_ENDPOINT = `${API_VERSION}/tags`

export const QUEUE_ENDPOINT = `${API_VERSION}/queue`

export const SETTINGS_ENDPOINT = `${API_VERSION}/settings`

export const UPDATE_ENDPOINT = `${API_VERSION}/update`

export const DELETE_ENDPOINT = `${API_VERSION}/delete`

export const CONFIG_ENDPOINT = `${API_VERSION}/config`
