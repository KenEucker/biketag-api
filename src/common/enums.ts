/// ****************************  BikeTag Enums   ************************************** ///
export enum AvailableApis {
    biketag,
    imgur,
    sanity,
    reddit,
    twitter,
  }
  
  export enum DataTypes {
    ambassador,
    game,
    player,
    setting,
    tag,
    queue,
  }

  export enum Errors {
      NotImplemented = "method not implemented for adapter: ",
  }